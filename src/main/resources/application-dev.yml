
spring:
  config:
    activate:
      on-profile: dev # 프로파일 설정


  datasource:
    url: jdbc:mysql://cook-assistant.cseedxgtmzcd.ap-northeast-2.rds.amazonaws.com:3306/vegan?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
      hibernate:
        ddl-auto: create
      properties:
        dialect: org.hibernate.dialect.MySQL5Dialect
        hibernate:
          format_sql: true

